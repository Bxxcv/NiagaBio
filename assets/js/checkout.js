document.addEventListener('DOMContentLoaded',async()=>{
  const params=new URLSearchParams(location.search); const username=params.get('username')||'demo'; const productId=params.get('product'); const profile=await NB.getProfileByUsername(username); if(!profile){ checkoutRoot.innerHTML='<div class="empty-state">Toko tidak ditemukan.</div>'; return; } const products=await NB.list('products',profile.user_id); const product=products.find(p=>p.id===productId)||products[0]; const checkout=(await NB.list('checkout_settings',profile.user_id))[0]||{}; if(!product){ checkoutRoot.innerHTML='<div class="empty-state">Produk tidak ditemukan.</div>'; return; }
  checkoutRoot.innerHTML=`<div class="row g-4 align-items-start"><div class="col-lg-5"><div class="card-nb p-4"><img src="${NB.escapeHtml(product.image_url||'assets/img/placeholder-product.svg')}" class="w-100 rounded-4 mb-3" alt=""><h1 class="h4 fw-bold">${NB.escapeHtml(product.name)}</h1><p class="text-muted">${NB.escapeHtml(product.description||'')}</p><div class="h3 text-green fw-black">${NB.money(product.price)}</div></div></div><div class="col-lg-7"><div class="card-nb p-4"><h2 class="h4 fw-bold mb-3">Checkout QRIS Manual</h2><p class="text-muted">Scan QRIS seller, lalu upload bukti pembayaran. Seller akan konfirmasi paid dari dashboard.</p>${checkout.qris_image_url?`<img class="qris-preview mb-3" src="${NB.escapeHtml(checkout.qris_image_url)}" alt="QRIS">`:`<div class="alert alert-warning">Seller belum upload QRIS. Gunakan WhatsApp.</div>`}<p class="small mb-4"><b>Nama QRIS:</b> ${NB.escapeHtml(checkout.qris_name||profile.display_name)}<br><b>Catatan:</b> ${NB.escapeHtml(checkout.payment_note||'-')}</p><form id="orderForm"><div class="row g-3"><div class="col-md-6"><label class="form-label">Nama pembeli</label><input id="buyerName" class="form-control" required></div><div class="col-md-6"><label class="form-label">No. WhatsApp</label><input id="buyerPhone" class="form-control" required></div><div class="col-md-6"><label class="form-label">Jumlah</label><input id="qty" type="number" min="1" value="1" class="form-control" required></div><div class="col-md-6"><label class="form-label">Upload bukti bayar</label><input id="proof" type="file" accept="image/*" class="form-control"></div></div><button class="btn btn-nb w-100 mt-4" type="submit">Kirim Pesanan</button><a class="btn btn-outline-nb w-100 mt-2" href="${NB.whatsappUrl(profile.whatsapp_number||checkout.whatsapp_number,'Halo kak, saya mau pesan '+product.name)}">Order via WhatsApp</a></form></div></div></div>`;
  orderForm.addEventListener('submit',async(e)=>{ e.preventDefault(); let proofUrl=''; if(proof.files[0]) proofUrl=await NB.uploadFile(proof.files[0],'proofs'); const quantity=Number(qty.value||1); await NB.save('orders',{id:NB.uid('ord'),seller_id:profile.user_id,buyer_name:buyerName.value.trim(),buyer_phone:buyerPhone.value.trim(),product_id:product.id,product_name:product.name,quantity,total_price:Number(product.price||0)*quantity,payment_method:'qris_manual',payment_status:'pending',proof_image_url:proofUrl,created_at:NB.now(),paid_at:null}); nbToast('Pesanan masuk. Tunggu seller konfirmasi paid.'); orderForm.reset(); });
});